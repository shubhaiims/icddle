{
  "name": "icddle-india",
  "version": "0.2.0",
  "private": true,
  "description": "ICDdle India frontend and file-backed adaptive backend prototype.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js && node --check backend/aiCaseGenerator.js && node --check api/daily-cases.js && node --check api/interactions.js && node --check api/health.js"
  }
}
